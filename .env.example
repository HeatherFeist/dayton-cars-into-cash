VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Pushover — instant phone alert to the owner on each new lead. Optional:
# if these are blank, the form still works and saves the lead; the owner
# just doesn't get a push. See PUSHOVER_SETUP.md for how to get these.
VITE_PUSHOVER_TOKEN=
VITE_PUSHOVER_USER=
